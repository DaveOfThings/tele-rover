[package]
name = "tele-rover"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
gilrs = { version = "0.11", default-features = false, features = ["xinput"] }
